# datasource.yml.j2
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://{{ prometheus | default("prometheus", true)}}:{{ prometheus_port | default(9090, true)}}
    isDefault: true
    version: 1
    editable: false
